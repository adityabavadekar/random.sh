For `mini.nvim`:

```bash
curl -fSL https://blueurl.vercel.app/mini.nvim | bash
```

Or to nuke all nvim related to avoid conflicts:

```bash
curl -fSL https://blueurl.vercel.app/mini.nvim | NUKE=true bash
```
